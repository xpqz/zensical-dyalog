# Target

Property

The Target property is a read-only character string that specifies the chosen folder or other resource selected by the user in a [BrowseBox](../objects/browsebox.md) object.

If the [BrowseFor](browsefor.md) is `'Directory'`, Target will contain a directory name followed by the character "\". Otherwise, Target just contains the name.

## Application

Objects: [BrowseBox](../objects/browsebox.md)
