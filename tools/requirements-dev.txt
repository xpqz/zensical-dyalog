# Dependencies of the conversion script and its tests. The documentation
# build stack (mkdocs, zensical and the plugins) is pinned in the source
# monorepo's tools/requirements-docs.txt; this file covers only what the
# tooling here needs on top of it.

# Script runtime: TOML serialisation, and HTML parsing for the heading rewrite.
tomli-w==1.2.0
beautifulsoup4==4.14.3

# Tests.
pytest==9.1.1

# The caption extension is packaged in pyproject.toml here so Zensical can
# import it by name at build time. Install it (and its markdown dependency)
# into the build/authoring environment with:  pip install -e tools/
